= render "header"
.account-page
  .account-page__inner.clearfix
    .account-page__inner--left.account-page__header
      %h2 Edit Account
      %h5 アカウントの編集
      = link_to "ログアウト",destroy_user_session_path, method: :delete, class: 'btn'
      = link_to "トップページに戻る", :back, class: 'btn'
    .account-page__inner--right.user-form
      = render "layouts/error_blog"
      = form_with(model: @blog, local: true) do |f|
        .field
          .field-label
            = f.label :name, "TITLE"
          .field-input
            = f.text_field :title
        .field
          .field-label
            = f.label :name, "URL"
          .field-input
            = f.url_field :url
        .field
          .field-label
            = f.label :紹介文
          .field-input
            = f.text_area :body
        .field
          .field-label
            = f.label :category
            = f.collection_select :category_id, Category.all, :id, :name
        .field
          .field__search--label
            %label.field__search--label--text タグを追加（クリックで選択、最大10個まで）
          .field__search--form
            %input.field__search--form--input#tag-form{autocomplete: "off"}
          #search-result
        .field
          .field__include--label
            %label.field__include--label--text 選択済（クリックで削除）
        = render "tags/form"
        .actions
          = f.submit "Update", class: 'btn'